version: '3.8'

services:
  telegram-forwarder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-forwarder
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
